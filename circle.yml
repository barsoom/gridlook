test:
  override:
    - script/ci/pipeline.sh gridlook_tests "bundle exec test"

deployment:
  apps:
    branch: master
    commands:
      - echo $HEROKU_APP_NAMES | sed s/,/\\n/g | xargs -I {} script/ci/pipeline.sh {}_deploy "script/ci/deploy.sh {}"
